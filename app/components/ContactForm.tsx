'use client'
import {FormEvent,useState} from 'react'
export default function ContactForm(){const [status,setStatus]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Sending…');const form=e.currentTarget;const data=new FormData(form);const email=String(data.get('email')||'');const message=String(data.get('message')||'');if(!/^\S+@\S+\.\S+$/.test(email)){setStatus('Please enter a valid email.');return}if(message.trim().length<10){setStatus('Please add a little more detail.');return}const r=await fetch('/api/contact',{method:'POST',body:data});const json=await r.json().catch(()=>({}));setStatus(r.ok?(json.message||'Message sent.'):(json.error||'Something went wrong.'));if(r.ok)form.reset()}return <form className="form" onSubmit={submit} noValidate><div><label htmlFor="name">Name</label><input id="name" name="name" required maxLength={80}/></div><div><label htmlFor="email">Email</label><input id="email" name="email" type="email" required maxLength={120}/></div><div className="honeypot" aria-hidden="true"><label htmlFor="website">Website</label><input id="website" name="website" tabIndex={-1} autoComplete="off"/></div><div><label htmlFor="message">Message</label><textarea id="message" name="message" required maxLength={2000}/></div><button className="submit" type="submit">Send message ↗</button><div className="status" aria-live="polite">{status}</div></form>}
